
test('TEST foo1', () => {
  const foo1 = require('../class.js')
  const ret = foo1()
  // exprect(ret)
  //   .toBe('test retrurn')
})

test('TEST foo2', () => {
  const foo2 = require('../class.js')
  const ret = foo2()
  // exprect(ret)
  //   .toBe('test retrurn')
})